base: base.yaml

experiment:
  name: oakink2_video
  modality: video

model:
  size: tiny      # overridden by sweep
  pretrained: true
  pretrained_path: null   # null = auto-resolve from pretrained_dir

training:
  total_epochs: 80
  learning_rate: 2.0e-4
  weight_decay: 0.01
